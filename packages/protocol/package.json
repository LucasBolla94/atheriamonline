{
  "name": "@atheriam/protocol",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Messages exchanged between the browser and the servers.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist *.tsbuildinfo"
  }
}
